# Workflow

- Never write code to files directly without confirmation. Always list proposed changes first and wait for approval.
- Enumerate all suggested changes as a numbered list. Only implement the changes the user specifies by number.
